# ============================================================
# 钻石架构验证项目 —— Python 依赖清单
# 作用：列出运行本项目需要的全部第三方库及版本要求。
# 安装方法（在 modelscope notebook 里执行一次即可）：
#     !pip install -r requirements.txt
#
# 重要说明：
#  1. transformers 必须 >= 5.2.0：Qwen3.5 系列（架构名 qwen3_5）
#     只有 transformers 5.2.0 能正确加载，5.3.0 目前会报错，不要升级更高。
#  2. modelscope 是"从魔搭下载模型/数据集"的官方库，notebook 通常已自带。
#  3. peft 用于跑 LoRA 对比基线实验（可选的对照组）。
# ============================================================

torch>=2.1.0
transformers==5.2.0
accelerate>=0.30.0
modelscope>=1.18.0
datasets>=2.19.0
peft>=0.13.0
numpy>=1.24.0
safetensors>=0.4.0
