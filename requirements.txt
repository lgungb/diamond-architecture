# ============================================================
# 钻石架构验证项目 —— Python 依赖清单
# 作用：列出运行本项目需要的全部第三方库及版本要求。
# 安装方法（在 modelscope notebook 里执行一次即可）：
#     !pip install -r requirements.txt
#
# 重要说明：
#  1. transformers 必须 >= 5.3.0：Qwen3.5 系列（架构名 qwen3_5）只有
#     transformers 5.3.0 及以上版本才认识这个架构（5.2.0 及以下会报
#     "model type qwen3_5 无法识别" 的错误）。
#  2. modelscope 是"从魔搭下载模型/数据集"的官方库，notebook 通常已自带。
#  3. qwen_vl_utils 是 Qwen 官方的多模态预处理工具包，纯文本任务用不到，
#     但装上可避免个别环境加载分词器/处理器时缺依赖（体积很小，装了无妨）。
#  4. peft 用于跑 LoRA 对比基线实验（可选的对照组）。
#  5. torch 必须 >= 2.5.0 且为【GPU 版】：transformers 5.x 要求 torch>=2.5，
#     否则会"禁用 PyTorch"；而且本实验要全参数微调 2B 模型，必须在 GPU
#     实例上运行（CPU 版 torch / 无显卡会慢到不可行）。请确保在魔搭的
#     GPU 免费实例（如 A10）里安装，不要在 CPU 实例上跑。
# ============================================================

torch>=2.5.0
transformers>=5.3.0
accelerate>=0.30.0
modelscope>=1.18.0
datasets>=2.19.0
peft>=0.13.0
numpy>=1.24.0
safetensors>=0.4.0
qwen-vl-utils>=0.0.14
