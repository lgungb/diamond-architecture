# ============================================================
# 钻石架构验证项目 —— git 忽略清单
# 作用：告诉 git 哪些文件/目录【不要】上传到 GitHub。
# 原因：模型缓存很大（好几个 GB），实验结果随时变，
#       这些都不适合放进代码仓库，只留"代码 + 文档"。
# ============================================================

# ---- Python 运行时产生的缓存目录 ----
__pycache__/
*.py[cod]

# ---- 模型缓存（snapshot_download 下载到本地的模型，很大，绝不提交）----
缓存/

# ---- 实验输出结果（补丁文件、模型状态、报告，都是运行后生成的）----
结果/

# ---- 临时文件 ----
*.tmp
*.log

# ---- 系统文件 ----
Thumbs.db
.DS_Store
