# 钻石架构验证实验（Diamond Architecture Validation）

> 用 **Qwen3.5-2B** 在魔搭（modelscope）免费 Notebook 上，验证"钻石架构"白皮书的几个核心论断：
> **全参数微调 → 只保存 Top 2%~6% 关键参数的差值（硬核补丁）→ 推理时盖回基座**，能否逼近全量微调的效果。

---

## 一、这是什么

- 这是**一份代码实验工程**，不是成品系统。它用来回答三个问题：
  1. 基座模型在任务上≈随机，全量微调后≈满分，那**只替换 2%~6% 的关键参数**，能恢复全量微调多少提升？
  2. 白皮书"策略A（高重要度）/ 策略B（低重要度）/ 策略C（混合）"三种筛选方法，到底哪种有效？
  3. 钻石架构 vs 业界常用的 **LoRA**，在"准确率 vs 文件体积"上谁更划算？
- 所有代码用**中文注释 + 中文命名**，不懂编程也能看懂流程。

## 二、快速开始（在 modelscope Notebook 里）

> ⚠️ **前提：必须用 GPU 实例。** 在魔搭【新建 Notebook】时选择 **GPU 免费实例**（如 A10 24G）。
> 本实验要全参数微调 2B 模型，CPU 实例（没有 nvidia-smi）跑不动，程序也会自动拦截并提示你换实例。

1. **把仓库拉到 Notebook**（在魔搭 Notebook 的终端里）：
   ```bash
   git clone <你的仓库地址>
   cd diamond-architecture
   ```
2. **装依赖**（只需一次）：
   ```bash
   pip install -r requirements.txt
   ```
   > ⚠️ 关键：`transformers` 必须装 **>= 5.3.0**（Qwen3.5 的 qwen3_5 架构只有 5.3.0 及以上才认识；5.2.0 及以下会报"模型类型无法识别"）。
   > ⚠️ 关键：`torch` 必须装 **>= 2.5.0 且为 GPU 版**（transformers 5.x 要求 torch>=2.5，否则会"禁用 PyTorch"；GPU 实例一般已自带，没有就执行 `!pip install torch --index-url https://download.pytorch.org/whl/cu124`）。
3. **先跑一次冒烟测试**（几分钟走通全流程，确认环境没问题）：
   ```bash
   python 运行/主入口.py
   ```
   如果跑之前想把规模调小，先改 `配置/配置.py` 里 `冒烟模式 = True`。
4. **跑正式实验**（约 30~90 分钟，取决于显卡）：
   - 把 `配置/配置.py` 里 `冒烟模式` 改回 `False`；
   - 再运行 `python 运行/主入口.py`；
   - 结果自动写到 `结果/报告.md`。

## 三、目录结构（每个文件干什么）

| 路径 | 作用 |
| --- | --- |
| `运行/主入口.py` | **唯一入口**。运行 `python 运行/主入口.py` 即可 |
| `配置/配置.py` | **参数总控**。改这里就能换模型、调规模、增删补丁 |
| `工具/环境自检.py` | 体检：检查 Python/显卡/依赖库/模型缓存 |
| `数据/合成数据.py` | 生成"三分类密语任务"（默认任务，离线可用） |
| `数据/真实数据.py` | 从魔搭下载真实分类数据集（可选，需自己填字段） |
| `数据/数据接口.py` | 数据统一入口 + 校验类别词 |
| `模型/模型加载.py` | 下载 Qwen3.5-2B + 自适应加载（自动兼容多模态） |
| `模型/模型工具.py` | 参数收集/状态存取/显存释放/DataLoader 等小工具 |
| `模型/微调.py` | 全参数微调训练器 + LoRA 训练 |
| `模型/计算重要度.py` | 用 Fisher 信息计算每个参数对任务的重要性 |
| `补丁/补丁格式.py` | 硬核补丁的数据结构与保存/读取 |
| `补丁/生成补丁.py` | 三种策略选位置 + 计算差值 → 生成补丁文件 |
| `补丁/应用补丁.py` | 把补丁盖回基座 + 测量切换耗时 |
| `评估/评估.py` | 计算分类准确率（裁判） |
| `实验/实验总控.py` | 总指挥：按顺序串起全部 10 步 |
| `实验/汇总报告.py` | 生成 `结果/报告.md` 对比表格与结论 |
| `requirements.txt` | 依赖清单 |
| `MEMORY.md` | **项目记忆**（给 AI 和人看的架构/流程说明） |

## 四、运行流程（10 步，自动完成）

```
加载基座A → 造数据 → 评估A(≈随机) → 算Fisher重要度 → 保存A
→ 全参数微调→专家B → 评估B(≈满分) → 生成各补丁 → 逐个盖回A评估+测切换耗时
→ LoRA对照组 → 汇总报告
```

## 五、常见问题

- **transformers 版本报错（不认识 qwen3_5 架构）？** 执行 `pip install "transformers>=5.3.0"`；仍不行再 `pip install git+https://github.com/huggingface/transformers.git`。
- **报"PyTorch was not found / PyTorch >= 2.5 is required"？** 说明 torch 没装或版本太旧（如 2.3.1）。先确认你在 GPU 实例上，再执行 `!pip install --upgrade torch --index-url https://download.pytorch.org/whl/cu124`，装完重启内核。
- **提示没有 GPU（nvidia-smi 不存在）？** 你当前是 CPU 实例，本实验跑不动。请到魔搭新建一个【GPU 免费实例】重新拉取仓库运行。
- **爆显存（CUDA out of memory）？** 默认已用内存友好的 Adafactor 优化器（不再用 AdamW）+ bf16 Fisher。如果还爆，把 `配置/配置.py` 里 `批次大小` 调到 `1~2`，`是否开启梯度检查点` 保持 `True`。
- **模型下载慢/失败？** 这是国内网络访问魔搭，一般很快；确认能访问 modelscope.cn。
- **"类别词不是单个token"报错？** 换 `配置.py` 里 `任务类别` 为常见的单字词。
- **本地电脑要不要跑？** 不需要。本地只改代码、提交 GitHub；跑实验在 modelscope Notebook 上。

## 六、结果怎么看

打开 `结果/报告.md`，核心看这张表：

| 模型变体 | 准确率 | 恢复率 |
| --- | --- | --- |
| 基座 A | ~33% | — |
| 全量微调 B | ~99% | 100% |
| 策略A_6% 补丁 | ??% | ??%（越接近 100% 越支持白皮书） |
| LoRA(r=16) | ??% | ??% |

- **恢复率高** → 说明"只替换 2~6% 参数逼近全量微调"成立。
- **策略B 准确率≈基座** → 验证"低重要度参数不改变核心能力"的说法。

---

*项目记忆见 `MEMORY.md`（AI 每次接手请先读它）。*
